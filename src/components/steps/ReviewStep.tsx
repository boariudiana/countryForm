



const ReviewStep = () => {
 

  return (
    <div>
      <h3>Review Your Information</h3>
      {/* <Descriptions bordered column={1}>
        <Descriptions.Item label="Country">
          {selectedCountry.name}
        </Descriptions.Item>
        
        <Descriptions.Item label={fields.personalId.label}>
          {formValues.personalInfo?.[fields.personalId.name]}
        </Descriptions.Item>

        <Descriptions.Item label={fields.region.label}>
          {formValues.personalInfo?.[fields.region.name]}
        </Descriptions.Item>

        {fields.postalCode && (
          <Descriptions.Item label={fields.postalCode.label}>
            {formValues.personalInfo?.[fields.postalCode.name]}
          </Descriptions.Item>
        )}

        {fields.visaType && (
          <Descriptions.Item label={fields.visaType.label}>
            {formValues.personalInfo?.[fields.visaType.name]}
          </Descriptions.Item>
        )}

        <Descriptions.Item label="Profile Image">
          {formValues.profileImage?.[0]?.name || 'No image uploaded'}
        </Descriptions.Item>
      </Descriptions> */}
    </div>
  );
};

export default ReviewStep; 